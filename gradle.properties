# Gradle options only. Mod metadata + per-version coordinates live in
# stonecutter.properties.toml (read via the `sc.properties` accessor).
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
# Stonecutter switches the active version by regenerating subprojects; configuration cache off.
org.gradle.configuration-cache=false
