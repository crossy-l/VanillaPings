# Gradle options + global mod metadata (per-version coordinates live in build.gradle.kts,
# keyed by the Stonecutter node's version).
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
# Stonecutter switches the active version by regenerating subprojects; configuration cache off.
org.gradle.configuration-cache=false

mod.id=vanillapings
mod.name=Vanilla Pings
mod.version=1.10.3
mod.group=com.vanillapings
